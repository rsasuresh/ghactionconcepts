name: workflow events used in giyhub actions

on:
  schedule:
    - cron: "*/1 * * * *"

jobs:
  testjob:
    runs-on: ubuntu-latest
    steps:
      - name: hellogreet
        run: echo "hello schedule_event"
